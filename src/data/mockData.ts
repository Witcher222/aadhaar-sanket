// Mock Data File - DEPRECATED / CLEARED
// This file previously contained fake data for demonstration.
// Detailed data is now strictly fetched from the real API based on uploaded files.
// Keeping exports to prevent build breakages during strict refactoring, but values are empty.

export const nationalStats = {
  totalEnrolments: 0,
  activeMigrations: 0,
  migrationVelocity: 'Low',
  dataFreshness: 'Unknown',
  lastUpdated: '-',
};

export const dailyEnrolmentData = [];
export const liveTicker = [];
export const migrationFlows = [];
export const sourceStates = [];
export const destStates = [];

export const trendData = {
  seasonality: [],
  persistence: [],
  acceleration: [],
};

export const stressZones = [];
export const heatmapData = [];
export const forecastAlerts = [];
export const forecastProjection = [];
export const policyRecommendations = [];

export const dataQualityMetrics = {
  integrityScore: 0,
  anomalyRate: 0,
  completeness: 0,
  timeliness: 0,
  accuracy: 0,
};

export const dataLineage = [];
export const aiSuggestedQueries = [];

export const demographicGroups = ["All"];
export const jobSectors = ["All Sectors"];
export const years = [];

